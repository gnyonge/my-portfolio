import React from "react";
import { Fade } from "react-awesome-reveal";

const Footer = () => {
  return (
    <div className="flex justify-center py-12 text-lg font-semibold">
      <Fade cascade>
        <p>읽어주셔서 감사합니다!</p>
      </Fade>
    </div>
  );
};

export default Footer;
